{
  "name": "warmup-design",
  "version": "1.0.0",
  "description": "Daily Design Warmups.",
  "scripts": {
    "start": "meteor --settings settings-development.json",
    "staging": "meteor deploy warmupdesign-staging.meteor.com --settings settings-development.json",
    "production": "meteor deploy warmupdesign.meteor.com --settings settings-production.json"
  }
}
